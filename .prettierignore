# Ignore everything
/*

# Except these files & folders
!/src
!/public
!/.github
!tsconfig.json
!astro.config.ts
!package.json
!.prettierrc
!eslint.config.js
!README.md
!.vscode

# Ignore MDX files
*.mdx

# 글은 포맷하지 않는다.
# prettier 는 마크다운 안의 코드블록 중 자기가 읽을 수 있는 언어(js·jsx·ts·css…)를
# 그냥 넘기지 않고 재작성한다. 옛 글 32 편에 한 번 돌려보니 React 예제의 코드블록
# 줄 수가 73→90, 129→138 로 바뀌었다. 2022 년에 그렇게 썼다는 것 자체가 글의 내용이라
# 지금 스타일로 고쳐 쓰면 안 된다.
/src/content/